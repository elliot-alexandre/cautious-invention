/**
 *
 * @param keyId is the discord Id of the user in the list saved with the information relative the ban word.
 */
export function EditUser(keyId: string) {}

export function CreateListUser(pathname: string, fileName: string) {}
